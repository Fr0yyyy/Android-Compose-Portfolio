package com.example.practica_evaluable_ej3.ui.theme

import androidx.compose.ui.graphics.Color

val TealPrimary = Color(0xFF00796B) // El verde azulado oscuro para la TopAppBar
val TealDark = Color(0xFF00695C)    // Un tono de acento para textos como la duración
val BackgroundGray = Color(0xFFF5F5F5) // Fondo general de la app, un gris muy claro
val CardBackground = Color.White       // Blanco para el fondo de las tarjetas

// Colores pastel para los chips de nivel, más suaves y fieles a la imagen
val LevelBasic = Color(0xFFE8F5E9)       // Verde pastel (Material Green 50)
val LevelIntermediate = Color(0xFFFFFDE7) // Amarillo pastel (Material Yellow 50)
val LevelAdvanced = Color(0xFFFFEBEE)     // Rojo pastel (Material Red 50)

// Gris claro para el fondo de los chips de etiquetas
val TagChipBackground = Color(0xFFECEFF1) // Material Blue Grey 50

// Colores para textos y iconos
val TextPrimary = Color.White       // Para textos sobre fondos oscuros (ej. TopAppBar)
val TextSecondary = Color(0xFF757575) // Gris oscuro para textos secundarios
